# Variables d'environnement pour TerraLens
# Copiez ce fichier vers .env.local et configurez vos vraies clés API

# Clé API Mapbox pour les cartes (obligatoire)
NEXT_PUBLIC_MAPBOX_API_KEY=your_mapbox_api_key_here

# Clé API OpenWeather pour les données météo (optionnel, l'app utilise Open-Meteo par défaut)
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Environnement
NODE_ENV=development